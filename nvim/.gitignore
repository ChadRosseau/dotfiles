# Neovim runtime state that does not belong in the repo
.luarc.json
**/.DS_Store
spell/

# NOTE: lazy-lock.json is intentionally NOT ignored.
# Tracking it is what makes this config reproducible on another machine
# (including a server you SSH into).
